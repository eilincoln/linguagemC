#include <stdio.h>
#include <stdlib.h>

int main()
{
    // decremento no laco for

    int contador;

    for(contador = 100; contador >= 0; contador--) {
        printf("numero: %d\n", contador);
    }
    return 0;
}
